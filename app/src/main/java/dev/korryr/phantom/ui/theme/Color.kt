package dev.korryr.phantom.ui.theme

import androidx.compose.ui.graphics.Color

// ─── PHANTOM STEALTH PALETTE ──────────────────────────────────────────────────

// Core brand — Electric Cyan (the ghost signal)
val PhantomCyan        = Color(0xFF00E5FF)   // primary accent — electric, sharp
val PhantomCyanDim     = Color(0xFF00B8D4)   // pressed / variant
val PhantomCyanDark    = Color(0xFF007B9A)   // dark mode container

// Dark backgrounds — deep space
val PhantomVoid        = Color(0xFF080B0F)   // darkest background
val PhantomAbyss       = Color(0xFF0D1117)   // main dark background
val PhantomSurface     = Color(0xFF161B22)   // card / surface dark
val PhantomElevated    = Color(0xFF1C2330)   // elevated surface dark
val PhantomBorder      = Color(0xFF21262D)   // subtle borders dark

// Light backgrounds — frosted glass feel
val PhantomFrost       = Color(0xFFF0F4F8)   // main light background
val PhantomWhite       = Color(0xFFFAFCFF)   // lightest surface
val PhantomSurfaceLight= Color(0xFFE8EFF5)   // card surface light
val PhantomBorderLight = Color(0xFFCDD5DF)   // subtle borders light

// Text
val PhantomGhost       = Color(0xFFE6EDF3)   // primary text dark mode
val PhantomMist        = Color(0xFF8B949E)   // secondary text dark mode
val PhantomShadow      = Color(0xFF161B22)   // primary text light mode
val PhantomSlate       = Color(0xFF5A6472)   // secondary text light mode

// Status colors
val PhantomGreen       = Color(0xFF3FB950)   // success / correct answer
val PhantomRed         = Color(0xFFFF4757)   // error / danger
val PhantomAmber       = Color(0xFFD29922)   // warning
val PhantomPurple      = Color(0xFF8957E5)   // tertiary accent

// Overlay specific (for the floating window)
val OverlayBackground  = Color(0xA6080B0F)   // 65% opacity black
val OverlayBorder      = Color(0x3300E5FF)   // 20% cyan border glow
val OverlaySurface     = Color(0xCC0D1117)   // 80% opacity surface